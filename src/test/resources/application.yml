server:
  name: name

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

  rabbitmq:
    host: host
    port: 0000
    username: username
    password: password

  application:
    name: name

excel-info:
  file-base-url: url
  excel-extension: extension

minio:
  bucket:
    name: name
  access:
    key: key
    secret: secret
  url: url

sse:
  timeout: 60000

rabbitmq:
  direct-exchange: direct-exchange
  creation-progress-exchange: creation-progress-exchange
  creation-request-queue: creation-request-queue

  creation-request-routing-key: creation-request-routing-key