spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

  rabbitmq:
    host: host
    port: 0000
    username: username
    password: password

  application:
    name: name

excel-info:
  file-base-url: url
  excel-extension: extension

minio:
  bucket:
    name: name
  access:
    key: key
    secret: secret
  url: url

sse:
  timeout: 60000

rabbitmq:
  direct-exchange: direct-exchange
  file-create-progress-exchange: file-create-progress-exchange
  file-create-request-queue: file-create-request-queue
  file-create-request-routing-key: file-create-request-routing-key